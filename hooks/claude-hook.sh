#!/usr/bin/env bash
# claude-hook.sh — Claude Code → HUD bridge (stub).
#
# This stub is intentionally empty. Phase 4 will implement the JSON
# normalization and HTTP POST to /api/events. Until then this file exists
# only so installers can wire the hook path into ~/.claude/settings.json.

set -euo pipefail

exit 0
